version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/stock_prediction
      - REDIS_URL=redis://redis:6379
      - MODEL_STORE_DIR=/app/model_store
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - PYTHONPATH=/app
      # Slower background sync so Yahoo isn't hammered
      - MARKET_SYNC_INTERVAL_SECONDS=600
      - INDEX_SYNC_STALE_SECONDS=120
      - NEWS_SYNC_STALE_SECONDS=3600
      - INSIGHT_SYNC_STALE_SECONDS=1800
    volumes:
      - ./model_store:/app/model_store
      # Mount live source so code changes take effect without rebuild
      - ./backend:/app/backend
    depends_on:
      - db
      - redis
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: stock_prediction
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.13.0
    ports:
      - "5001:5000"
    command: >
      mlflow server
        --host 0.0.0.0
        --port 5000
        --default-artifact-root /mlflow/artifacts
        --backend-store-uri sqlite:///mlflow/mlflow.db
    volumes:
      - mlflow_data:/mlflow
    restart: unless-stopped

volumes:
  postgres_data:
  mlflow_data:
